# Copy to .env and fill values
# MongoDB connection string (MongoDB Atlas recommended)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/todo
# Optional: port the backend should listen on
PORT=8080
